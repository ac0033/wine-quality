# 第三步主 agent 验收

**结论：通过。** 固定的训练、验证、最终评估划分和训练中位数验证基线已建立，可供后续研究性回归实验沿用。最终评估集尚未计分，用户理解状态待确认，第四步未启动。

## 从实际产物与原始数据验收

- 检查 `src/prepare.py`、`checks/verify.py`、`outputs/split_assignments.csv`、`outputs/split_summary.json`、`outputs/baseline_validation.json`、`execution.jsonl` 和 `report.md`。执行者从原始 CSV 全行重建划分与基线的独立复核为 26,081/26,081 项通过。
- 主 agent 用 PowerShell 从两份原始 CSV 和最终划分表独立核对：红酒 1,599 行、白酒 4,898 行，每个源文件行号恰好出现一次；相同特征组合跨划分数为 0。红酒训练/验证/最终评估为 950/331/318 行；白酒为 2,921/983/994 行，与划分摘要和报告一致。
- 主 agent 仅从训练行重算 `quality` 中位数，红酒和白酒均为 6.0；仅从验证行重算红酒 MAE 0.6435045317、RMSE 0.8777201999，白酒 MAE 0.6368260427、RMSE 0.9032551059，与 `baseline_validation.json` 数值一致。该文件只有训练和验证指标，代码没有计算最终评估集预测或误差。
- 主 agent 独立核对报告中的红酒第 1 行：11 个理化指标生成的特征组合 ID 与划分表一致；在红酒 `quality=5` 的 577 个组合中固定排序第 145，属于验证集。这条记录的基线预测为 6.0，实际 `quality=5`，绝对差为 1.0，说明单行怎样进入验证 MAE。
- 主 agent 用当前磁盘文件独立核对七份只读输入的字节数与 SHA256，均与第三步 `split_summary.json` 的执行前、执行后记录一致。Git 状态仅显示新增 `steps/`，没有原始数据的跟踪改动。

## 验收边界与历史记录

当前任务仅为研究性回归，预测时点假定 11 个理化指标已测得而专家评分未知。没有样本 ID 或采集时间，按完全相同特征组合分组无法排除其他样本依赖或证明未来泛化。验证集基线只是后续比较起点，不证明理化指标有预测收益；最终评估集必须继续留到最终评估。

第一步 `acceptance.md` 在第一步收尾时的文件清单为 1,989 字节，第二步开始及第三步前后为 1,785 字节；当前版本少了一条关于第一步复现命令更正的说明，变化来源尚不清楚。第一步 `data_profile.json` 与三份原始数据的指纹未发现变化。此历史差异不改变本步划分和基线的独立复算结果，仍需保留记录，不反向覆盖旧产物。
