# 11.1 验收

**结论：执行通过。** 两个模型版本登记为候选，交付清单齐全，打分入口跑通。模型状态停在候选，验收与交付由用户在平台上决定。

## 核对

- **登记的文件就是 7.1 的产物**：`红酒_quality_随机森林 8314f626bc19`、`白酒_quality_随机森林 32bfb8ca1cde` 的版本号是模型文件 SHA256 前 12 位，与 9.1 输入指纹里记录的四个模型文件哈希一致；notebook 里没有任何 `fit`。
- **选的是 9.1 最终评估 MAE 最低的候选**：代码从 `final_evaluation.json` 取每类酒 MAE 最低的候选并断言是随机森林；Ridge 文件保留在 7.1，未登记。
- **打分入口真的跑了**：notebook 用子进程调用 `src/predict.py`，对每类酒 10 行示例输出 10 行、`quality_估计` 无空值；输入文件只含 11 个理化指标列，没有 `quality`。
- **交付清单**：`dsflow delivery check` 两份都齐全——版本与登记一致、数据版本与指标数值与产出运行一致、模型文件在、没有「待填」；再跑一次 `dsflow delivery init` 刷新后人写部分保留、核对仍通过。
- **指标来源**：清单里的 `MAE_最终评估` 数值由这次运行的 `log_metrics` 带入，数值原样取自 9.1 的产物，没有重算；两类酒的指标登记在同一次运行里，所以两份清单都列了四个指标，`scope` 里写明哪一个属于本清单的模型。

## 边界

- 交付的是研究结论和可复现的打分入口，没有推理服务；部署方式待定。
- 复现入口里的 `git checkout` 指向运行时的提交（当时 steps/ 尚未提交，运行记录标为有未提交改动）；本轮全部提交后，重新 `dsflow delivery init` 会刷新到新的提交。
