# 11.1 报告：模型登记与交付清单

**本步执行通过。** 每类酒登记了一个模型版本（状态：候选）：`红酒_quality_随机森林` **8314f626bc19**、`白酒_quality_随机森林` **32bfb8ca1cde**，文件就是 7.1 只用训练行拟合的随机森林，没有重训。两份交付清单已写完并通过平台核对；打分入口对 20 条示例记录跑通。是否验收、是否交付，由你在平台「模型与交付」页决定。

## 交付的是什么

- 模型文件：7.1 的 `outputs/models/red_随机森林.joblib`、`white_随机森林.joblib`，按内容哈希登记为版本；平台把训练数据版本、代码版本和 9.1 的最终评估指标关联到登记它的这次运行。
- 打分入口：[`src/predict.py`](src/predict.py)。给一份含 11 个理化指标列的 CSV（分号或逗号分隔都行），输出原始列加 `quality_估计`（连续值，不取整）；红酒用红酒的模型、白酒用白酒的，不混用。[示例输出](outputs/scoring_example.csv)是用最终评估集每类酒前 10 行跑的，附真值 `quality` 只供对照，不是新的评估。
- 交付清单：[红酒](../../../delivery/红酒_quality_随机森林/checklist.yaml)、[白酒](../../../delivery/白酒_quality_随机森林/checklist.yaml)。平台填了版本、复现入口、数据版本、指标数值；我填了一句话说明、使用入口、适用边界、监控方案、部署方式（待定）和遗留问题。两份都通过 `dsflow delivery check`。

## 使用边界（清单里的核心几条）

- 只对葡萄牙 Vinho Verde 的红酒 / 白酒、11 个理化指标齐全的记录成立；适合批量粗筛或排序，不能把估计值当作专家评分本身。
- 最终评估 MAE 红酒 0.4963、白酒 0.5752（基线 0.6855、0.6469）；两端分值误差在 1.2～2.9 分，quality 恰好 6 分的酒上不如一律猜 6 分。
- 模型主要靠 `alcohol`（10.1）；最终评估行在 2.1 被全量探索看过，对未来样本的效果没有验证。
- 监控：新记录拿到专家评分后算 MAE，红酒高于 0.60、白酒高于 0.69（最终评估值的 1.2 倍）就回到 7.1 重新比较候选、登记新版本。
- 没有生产场景与业务成本依据：「优于基线」不等于「值得使用」；白酒 Ridge 没站住，只交付随机森林。

## 证据

[notebook](nb_11.1.ipynb) 在运行记录里存了登记的候选、模型文件哈希前 12 位和 9.1 的最终评估 MAE；打分入口是通过子进程真的执行的，输出行数等于输入行数、没有空的估计值。模型登记后 `dsflow model list` 显示两个候选版本；`dsflow delivery check` 两份清单齐全，部署方式「待定」作为备注不算未通过。验收细节见[验收报告](acceptance.md)。
